

class CellbaseClient{
   private readonly API_URL = "http://cellbase.clinbioinfosspa.es/cellbase-4.6.3/webservices/rest/v4";

   private readonly get_gene_id = async (geneStr: string)  => {
      // process parameters and setup call

      try {
         // fetch()

      } catch (error) {
         // return Promise.reject(err)
      }
   };
};


export default CellbaseClient;